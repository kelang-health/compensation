# ระบบค่าตอบแทน

Google Sheets เป็นฐานข้อมูล, Google Apps Script เป็น API และ GitHub Pages เป็นหน้าแสดงผล โดยมี Cloudflare Worker เป็น API gateway

ข้อมูลลับ, รหัสผ่าน, Spreadsheet ID, session token และข้อมูลบุคลากรต้องไม่อยู่ใน GitHub

ดู `docs/DEPLOYMENT.md` เพื่อการตั้งค่าและ deployment.